// 编程练习06 -- 计算并打印一个变量的两倍与平方
#include <stdio.h>

int main(void)
{
    int tose;

    tose = 10;
    printf("tose = %d\n", tose);
    printf("t double = %d\n", tose * 2);
    printf("t square = %d\n", tose * tose);

    return 0;
}